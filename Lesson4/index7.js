const perimeter = {
    a: 5,
    b: 10,
};

function perimeterRectangle(perimeter) {
    return perimeter.a * perimeter.b;
    return (perimeter.a + perimeter.b) * 2;
}

console.log(perimeterRectangle(perimeter));
